server:
  port: 10001

spring:
  datasource:
    hikari:
      driver-class-name: com.mysql.cj.jdbc.Driver
      jdbc-url: jdbc:mysql://localhost:3306/rscm?serverTimezone=UTC&characterEncoding=UTF-8
      username: root
      password: dntjddms522@
  thymeleaf:
    check-template: false

logging:
  level:
    hello.rscm: debug